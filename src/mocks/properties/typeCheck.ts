/**
 * Type check json data
 */
import { AvbPropertiesResponse } from '@features/Offers';
import properties from './data.json';

const typeCheckProperties = (a: AvbPropertiesResponse) => null;
typeCheckProperties(properties);
