/**
 * Type check json data
 */
import { OfferConfirmation } from '@features/Offers';
import postData from './data.json';

const typeCheckAdvices = (a: OfferConfirmation) => null;
typeCheckAdvices(postData);
